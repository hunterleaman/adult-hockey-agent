# Project Memory

Facts-only workstream memory. One-line pointers to `memory/prompt_<slug>.md`
resume files written by `/wind-down`. Record verified state only -- no
speculation, no narrative. `/orient` and `/wind-up` load this file plus the
pointed-to resume prompt every session, so keep it short and cheap to read.

## Active Workstreams

<!-- One line per active workstream, e.g.:
     - <slug>: <phase/status> -> memory/prompt_<slug>.md
     Delete the line when the workstream closes. -->
